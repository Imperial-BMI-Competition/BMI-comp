load('monkeydata_training.mat')
for D = 1:size(trial,2)
    for T = 1:size(trial,1)
        X = trial(T,D).handpos(1,:);
        Y = trial(T,D).handpos(2,:);
        if D = 1
            C = 'y'
        else if D = 2
                
        plot(trial(T,D).handpos(1,:)